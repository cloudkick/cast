var swiz = require('./lib/swiz');
var s = new swiz.Swiz({});
var obj = {
  data: {
    foo: 'one',
    bar: 'two'
  }
};

s.buildObject(obj, function(err, result) {
  console.log(s.serializeXml(result));
});
